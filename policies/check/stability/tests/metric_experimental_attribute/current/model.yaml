version: '2'
attributes:
  - key: experimental.attr
    brief: experimental attribute
    type: string
    stability: development
  - key: stable.attr
    brief: stable attribute
    type: string
    stability: stable
metrics:
  - name: stable.metric
    brief: stable metric
    stability: stable
    instrument: counter
    unit: "1"
    attributes:
      - ref: experimental.attr
        requirement_level: required
      - ref: stable.attr
        requirement_level: required
      - ref: experimental.attr
        requirement_level: 
          opt_in: "this is okay"
